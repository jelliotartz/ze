<link rel="stylesheet" href="https://rawgit.com/enyo/dropzone/master/dist/dropzone.css">

<h2>Enter your text below:</h2>
<div id="text_form">
	<%= form_for :sample, {url: analyze_path, remote: true, html: { class: "content-input" } } do |f| %>
	<%= f.text_area :content %>
	<%= f.submit "Submit" %>
	<% end %>
</div>
<a id="toggle-form" href="#">Analyze <span id="toggle-text">Twitter</span> Sample</a>
<div id="twitter_form">
	<%= form_for :tweet, {url: analyze_path, remote: true, html: { class: "content-input" } } do |f| %>
	<%= f.label "Enter a twitter handle"  %>
	<%= f.text_field :content %>
	<%= f.submit "Submit" %>
	<% end %>
</div>


<div>
	<hr>
	<%= form_tag({action: :analyze}, {multipart: true, class: "dropzone", id:"drop-form"}) do %>
<% end %>
</div>

<div id="output">
</div>
