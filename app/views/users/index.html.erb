<div>
	<%= link_to "Back to Home", welcome_path %>
</div>

<div>
	<ul>
	  <% @users.each do |user| %>
	    <li>
	      <%= link_to user.email, user_path(user)  %>
	    </li>
	  <% end %>
	</ul>
</div>
